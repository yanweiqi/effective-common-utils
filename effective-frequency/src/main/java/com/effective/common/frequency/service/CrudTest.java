package com.effective.common.frequency.service;
/*
import org.springframework.boot.test.context.SpringBootTest;

import javax.annotation.Resource;

@SpringBootTest
public class CrudTest {

    @Resource
    private AppMapper appMapper;

    @Test
    public void insert() {
        App user = App.builder()
                .appName("yyy")
                .appSwitch("0")
                .appVersion("333")
                .build();
        assertThat(appMapper.insert(user)).isGreaterThan(0);
        // 成功直接拿回写的 ID
        assertThat(user.getId()).isNotNull();
    }

}*/