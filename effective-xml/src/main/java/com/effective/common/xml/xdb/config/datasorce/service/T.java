package com.effective.common.xml.xdb.config.datasorce.service;

import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

public class T extends Thread{
	
	@SuppressWarnings("unused")
	private static final Logger logger = LoggerFactory.getLogger(T.class);

	public void run(){
//		String thread_name  = Thread.currentThread().getName();
//		String thread_info  = Thread.currentThread().toString();
//		long   thread_id    = Thread.currentThread().getId();
//		Student s = ContextHolder.getStudent();
//        Random random = new Random();  
//        int age = random.nextInt(100); 
//        s.setAge(age);
//        for (int i = 0; i < 2; i++) {
//        	logger.info("-->>thread name:"+thread_name +",thread_id:"+thread_id+",thread_info:"+thread_info +",student:"+s.toString());
//		}
		
	}
}
